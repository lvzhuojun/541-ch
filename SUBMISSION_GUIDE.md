# Submission Guide

This repository is organized around the three actual submission targets for the assignment.

## 1. Kaggle

Upload these three CSV files separately, one file per Kaggle submission:

1. `submissions/Zhuojun Lyu_Part_1.csv`
2. `submissions/Zhuojun Lyu_Part_2.csv`
3. `submissions/Zhuojun Lyu_Part_3.csv`

The CSV format matches the assignment requirement:

```csv
id,label
distortion00_1_0,79
```

## 2. Colab / Notebook Deliverable

Use:

- `cs541_challenge_three_parts.ipynb`

This is the notebook version of the workflow that can be uploaded, shared, or opened in Colab as required by the course.

## 3. Gradescope / Report

Use:

- `report/CS541_Report_Draft_For_Word.md`

The plan is to move this draft into Word and export the final PDF from there. The report figures and analysis tables needed by the write-up are already included under `report/`.

## Notes on the Selected CSV Versions

This repo intentionally keeps a controlled-score Kaggle submission set:

- Part 1: standard final Part 1 CSV
- Part 2: `v070` CSV
- Part 3: `v080` CSV

The report section still documents the strongest local model behavior where needed for the assignment discussion and analysis.
